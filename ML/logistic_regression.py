# WORK IN PROGRESS

import numpy as np


def main():
    m = int(input("Enter m,the number of input samples"))

    for i in range(n):
        txt = "Enter the {value}th example in the training set."
        xi, yi = input(txt.format(value=i)).split()

        X.append(int(xi))
        Y.append(int(yi))

    for j in range(0, m):
        theta[j] = 0
        gradient[j] = 0

    for i in range(0, m):
        pass


if __name__ == "__main__":
    main()
